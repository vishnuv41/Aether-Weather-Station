Put your **rotated** AWS IoT certificate files here (never the ones pasted in chat):

- AmazonRootCA1.pem
- device-cert.pem.crt
- private.pem.key

This folder is gitignored — these files should never be committed.
